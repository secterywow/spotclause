# LangChain / LangGraph
langchain~=1.0
langgraph~=1.0
langchain-core~=1.0
langgraph-checkpoint>=2.1.0
langgraph-cli[inmem]
langchain_openai

# Web Framework
fastapi~=0.115
uvicorn[standard]~=0.34
python-multipart~=0.0.20

# Database
sqlalchemy~=2.0
alembic~=1.14
psycopg2-binary~=2.9

# Task Queue
celery[redis]~=5.4
redis~=5.2

# Config & Validation
pydantic[email]~=2.0
pydantic-settings~=2.7
python-dotenv~=1.0
email-validator~=2.2

# File Parsing
pdfplumber~=0.11
python-docx~=1.1
pytesseract~=0.3.13
Pillow~=11.1

# PDF Generation (reportlab ships Adobe's CJK CIDFonts, so we get
# Chinese / Japanese / Korean text in exports without bundling font files.)
reportlab~=4.2

# Auth
PyJWT~=2.10
passlib[bcrypt]~=1.7
python-jose[cryptography]~=3.3

# HTTP Client
httpx~=0.28

# Payment
stripe~=11.0

# Email
resend~=2.30

# Google OAuth
google-auth~=2.38

# Logging
structlog~=25.1

# Utilities
python-dateutil~=2.9
